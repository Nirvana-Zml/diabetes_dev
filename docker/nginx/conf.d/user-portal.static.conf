# 用户端静态资源（Ngrok / 演示）：http://localhost/
server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html/user;
    index index.html;

    include /etc/nginx/conf.d/snippets/api-proxy.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
