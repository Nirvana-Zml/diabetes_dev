<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.diabetes.audit.mapper.AuditActionDefinitionMapper">

    <select id="findAllEnabled" resultType="com.diabetes.audit.entity.AuditActionDefinition">
        SELECT ACTION_CODE AS actionCode,
               LABEL_ZH AS labelZh,
               CATEGORY,
               IS_SYSTEM AS isSystem,
               ENABLED,
               SORT_ORDER AS sortOrder,
               CREATED_AT AS createdAt
        FROM AUDIT_ACTION_DEFINITIONS
        WHERE ENABLED = 1
        ORDER BY SORT_ORDER, ACTION_CODE
    </select>

</mapper>
