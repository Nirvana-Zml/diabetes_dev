# 注册性能测试实验结果记录表

> **测试接口**: `POST /api/v1/auth/register`  
> **测试脚本**: `测试脚本/用户注册压测.py`

---

## 测试输入

### 测试环境

| 项目 | 内容 |
|------|------|
| **目标服务器** | `http://192.168.22.28` |
| **测试接口** | `POST /api/v1/auth/register` |
| **测试脚本** | `测试脚本/用户注册压测.py` |
| **压测工具** | WebRunner |
| **压测节点** | 192.168.22.28:50000 × 1 节点（主节点） |

### 压测场景参数

| 项目 | 内容 |
|------|------|
| **负载模式** | 梯形负载 |
| **峰值并发用户数** | 50 |
| **用户创建速率** | 10 用户/秒 |
| **压测运行时长** | 300 秒 |
| **虚拟用户总数** | 100（按创建速率递增至峰值后维持） |

### 请求规格

| 项目 | 内容 |
|------|------|
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **User-Agent** | Chrome 114.0.0.0 / Windows 10 x64 |

### 请求体参数

| 参数 | 类型 | 生成规则 | 示例 |
|------|------|----------|------|
| `username` | string | `load_{userId}_{seq}`，3-50 字符 | `load_1_854495` |
| `phone` | string | `158` + 8 位序号，11 位，满足 `^1[3-9]\d{9}$` | `15800000001` |
| `password` | string | 固定值 `Test123456`，6-32 字符 | `Test123456` |

### 校验规则

| 检查项 | 规则 | 判定标准 |
|--------|------|----------|
| HTTP 状态码 | `== 200` | 通过 / 失败 |
| 业务状态码 | `body.code == 200` | 通过 / 失败 |
| 响应格式 | 合法 JSON | 通过 / 失败 |

### 线程安全机制

| 措施 | 说明 |
|------|------|
| 序号生成 | `threading.Lock` 保护全局计数器，确保 `username`、`phone` 并发唯一 |
| 失败标记 | 非 200 响应调用 `res.failure()`，不计入成功数 |

---

## 基本信息

| 项目 | 内容 |
|------|------|
| **测试人员** | |
| **测试时间** | 2026-07-01 21:00:54 ~ 21:01:06 |
| **测试结果** | ✅ 通过（100次请求全部成功） |

---

## 系统注册接口平均响应时间（单位：秒）

| 指标 | 数值 |
|------|------|
| 平均响应时间 | 0.074 |

---

## 并发压测数据

| 并发用户数 | 平均响应时间（秒） | 成功数 | 失败数 |
|-----------|-------------------|--------|--------|
| 50（峰值）/ 100（总启动） | 0.074 | 100 | 0 |

---

## 备注

> 在此记录测试过程中的异常情况、环境配置或其他补充说明。
>
> - 测试环境：`http://192.168.22.28`
> - 压测模式：梯形负载（峰值并发 50 用户，创建速率 10 用户/秒，运行时长 300 秒）
> - 密码："Test123456"
> - 校验规则：username 3-50 字符 / phone 11 位 1[3-9] 开头 / password 6-32 字符